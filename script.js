function setup() {
    createCanvas(windowWidth, windowHeight);
    // var startX, startY, endX, endY;
    // startX = windowWidth/2;
    // startY = windowHeight/2;
    // endX = startX;
    // endY = startY;
    this.test = new Cluster(50, windowWidth / 2, windowHeight / 2);
    background(0);
    this.test.show();
}

function draw() {

}